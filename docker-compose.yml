services:
  postgres:
    image: postgres:17-alpine3.21
    container_name: supernova-postgres17
    environment:
      POSTGRES_PASSWORD: postgres
    ports:
      - '5432:5432'
    volumes:
      - './db:/var/lib/postgresql/data'
