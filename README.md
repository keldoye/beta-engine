# .env file structure =
### db
DB_USERNAME=
DB_PASSWORD=
DB_HOST=
DB_PROXY_PORT=
DB_POSTGRES_PORT=
DB_NAME=
DATABASE_URL=optional

### auth
JWT_SECRET=

### Running the backend:
run the following commands:
- docker compose up -d
- nest start --debug