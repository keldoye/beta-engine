import { Person } from 'src/modules/contact/person/person.entity';
import { User } from 'src/modules/contact/person/user/user.entity';

//Add the db tables in the object below
export const DatabaseEntities = {
	User,
	Person
};
